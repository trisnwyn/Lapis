# Lapis Extension (M5)

Chrome MV3 extension — cầu nối giữa LMS của trường và webapp Lapis:

- v0.1: bắt lịch học (fetch-patch trên trang LMS, fallback DOM scrape) → đẩy vào webapp
- v0.2: side panel trợ lý học trên mọi trang

Sẽ được triển khai ở bước M5. Logic parse nằm ở `packages/lms-vcbi`.
