# Lapis — biến môi trường build (chỉ admin cần cấu hình)

# Google OAuth Client ID (Web) thuộc project của admin.
# Origins phải chứa: localhost (dev), domain Vercel production + preview cần dùng.
# Người dùng cuối KHÔNG cần làm gì — client ID không phải secret.
VITE_GOOGLE_CLIENT_ID=
